# Base URL for the Django API (no trailing slash).
# Local (CORS): http://localhost:8000
# Local (Vite proxy to backend): leave empty — /api is proxied in vite.config.js
# Production: https://your-backend.onrender.com  (or your API host)
VITE_API_BASE_URL=http://localhost:8000
