# Base URL for the Django API (no trailing slash).
# Dev with CORS: http://localhost:8000
# Dev with Vite proxy only: leave empty — requests use same origin and /api is proxied.
VITE_API_BASE_URL=http://localhost:8000
